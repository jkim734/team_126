# 웹사이트 로컬 실행·검수 안내

## 1. 무엇을 설치하나요?

일반 이용자는 아무것도 설치하지 않습니다. 이 프로젝트를 내 PC에서 시험하는 사람만
**Python 3.13(64비트)**을 사용합니다. 이미 Python 3.13이 있다면 새로 설치할 필요가
없습니다. Git, Docker, PyQt는 필요하지 않습니다.

1. Python이 없다면 공식 Python install manager를 설치한 뒤 `py install 3.13`을 실행합니다.
2. 설치 첫 화면에서 **Add python.exe to PATH**를 체크합니다.
3. **Install Now**를 누르고 설치를 마칩니다.
4. PowerShell을 새로 열고 `python --version` 또는 `py -3.13 --version`을 실행합니다.
5. `Python 3.13.x`가 보이면 준비 완료입니다.

## 2. 웹사이트를 켜는 가장 쉬운 방법

이 폴더의 빈 공간에서 Shift+마우스 오른쪽 버튼을 누르고 **터미널에서 열기**를 선택한
뒤 아래 명령을 붙여넣습니다. 웹의 모든 기능을 검수하려면 인증키 입력은 필수입니다.

```powershell
powershell -ExecutionPolicy Bypass -File .\로컬_실행.ps1
```

첫 실행에서는 프로젝트 폴더의 `.venv313` 안에 Flask를 자동 설치합니다. 시스템 전체에는
설치하지 않습니다. Python이 인증키를 직접 입력받으므로 입력 문자가 화면에 보이지 않는 것은 정상입니다. 인증키는
파일에 저장되지 않고, 서버를 끄면 사라집니다.

서버가 시작되면 브라우저에서 <http://127.0.0.1:8013>을 엽니다. 서버 종료는 터미널에서
`Ctrl+C`입니다.

## 3. 환경변수가 무엇인가요?

환경변수는 인증키를 소스코드에 적지 않고 실행 중인 컴퓨터나 서버만 알고 있게 하는
이름표가 붙은 비밀 보관함입니다. 이 프로젝트에서는 이름이
`DATA_GO_KR_API_KEY`입니다. 환경변수를 쓰는 것은 배포를 막는 문제가 아니라, 공개
웹사이트에서 인증키가 방문자에게 유출되지 않도록 하는 정상적인 배포 방식입니다.

## 4. 민수님 검수 체크리스트

다음 항목을 PC 브라우저와 휴대폰 크기 창에서 각각 확인합니다.

- [ ] 첫 화면이 깨지지 않고 `버릴지도` 제목이 보인다.
- [ ] 종로구·성북구·장안구·팔달구·권선구가 모두 선택된다.
- [ ] 구를 바꾸면 해당 동 목록과 생활쓰레기 일정이 함께 바뀐다.
- [ ] `깨진 유리`, `우유팩`, `매트리스` 검색 결과가 나온다.
- [ ] 등록되지 않은 임의 품목은 찾지 못했다는 안내가 나온다.
- [ ] 폐의약품·폐건전지·폐형광등 탭마다 목록과 지도 마커가 나온다.
- [ ] **내 위치로 지역 찾기**를 누르면 가장 가까운 지원 구·동이 자동 선택되고 일정이 바뀐다.
- [ ] 이미 검색한 품목·대형폐기물 결과가 새 지역 기준으로 다시 조회된다.
- [ ] 수거함이 현재 위치와 가까운 순으로 나오고 거리 숫자가 표시된다.
- [ ] 대형폐기물 `의자`를 검색하면 수수료와 공식 신고 링크가 나온다.
- [ ] 공식 신고 링크가 새 탭에서 열린다.
- [ ] 인증키를 넣지 않고 실행했을 때 수거함·수수료에 관리자 안내가 나온다.

## 5. 현재 검증된 범위

- Python과 JavaScript 문법 검사 통과
- `/health`, 지역, 일정, 품목 검색 API 정상 응답
- 인증키 누락 시 수거함·수수료 API가 친화적인 오류로 응답
- 데스크톱 및 390px 모바일 화면 렌더링, 가로 넘침 없음
- 브라우저 콘솔 오류 없음
- 프론트 HTML/JavaScript에 공공데이터 인증키 이름·값 미노출

실제 공공데이터 3종과 현재 위치 거리순 결과는 본인의 공공데이터 인증키를 입력한 뒤
체크리스트로 최종 확인해야 합니다.

## 6. 다음 담당자에게 넘길 파일

배포 담당자는 `HANDOFF.md`부터 읽으면 됩니다. 최소 전달 대상은 다음과 같습니다.

- `web_app.py`, `waste_api.py`, `run_local.py`
- `templates/`, `static/`
- `requirements-web.txt`, `render.yaml`
- `HANDOFF.md`, `.gitignore`

`.venv`, `.venv313`, `__pycache__`, `의견1.png`, `의견2.png`, `front.py`는 웹 배포에 필요하지 않습니다.
